puppet_module:
  paths:
    - src/puppet_module.rs

ci:
  paths:
    - .github/workflows/*

docs:
  paths:
    - README.md
    - CONTRIBUTING.md
    - docs/**

dependencies:
  paths:
    - Cargo.toml
    - Cargo.lock

tests:
  paths:
    - tests/**
    - src/puppet_module.rs

release:
  paths:
    - Release.toml

pre-commit:
  paths:
    - .pre-commit-config.yaml

deny:
  paths:
    - deny.toml
